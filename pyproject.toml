[project]
name = "agentos"
version = "0.2.0"
description = "AgentOS control plane reference implementation"
requires-python = ">=3.11"
license = {text = "Apache-2.0"}

[tool.ruff]
line-length = 100
target-version = "py311"
